{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}

 {{title}}
 {{productObject.id}}
 {{productObject.name}}
 {{productObject.price}}
 {% set prixtotale = 0 %}
{% if productsArray|length != 0 %}
<table class="table">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Price</th>
    </tr>
{% for product in productsArray %}
{% set prixtotale = prixtotale+product.price %}
<tr>
    <td>{{product.id}}</td>
    <td>{{product.name|upper}}</td>
    <td>{{product.price}}</td>
</tr>
  {%endfor%}
</table >
{% else %}
<p>There are no products</p>
{% endif %}
{% apply upper%}
Prix des produits = {{prixtotale}}
{%endapply%}
{% if 'abien' starts with 'F' %}
  commence par F
{% endif %}
{% if 5 not in[1, 2, 3,5] %}
  5 non présent
{% endif %}
<a href="{{path('showTeacher',{'name':'nader','id':1})}}">Vers teacher</a>

<img src="/images/image1.jpg" width="250" height="250">

{% endblock %}
